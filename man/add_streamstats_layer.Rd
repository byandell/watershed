% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streams.R
\name{add_streamstats_layer}
\alias{add_streamstats_layer}
\title{Add StreamStats Layer and Vector Stream Flowlines to Leaflet Map}
\usage{
add_streamstats_layer(
  map,
  lat,
  lng,
  rcode,
  basin_group = "StreamStats Basin",
  stream_group = "Stream Flowlines",
  layer_group = NULL,
  stream_color = "#0055ff",
  basin_color = "#3388ff",
  color = NULL,
  weight = 3,
  opacity = 0.9,
  fillOpacity = 0.15,
  include_flowlines = TRUE,
  buffer_km = 3,
  ...
)
}
\arguments{
\item{map}{A \code{leaflet} map or \code{leafletProxy} object.}

\item{lat}{Numeric. Latitude of the delineation pour point (in WGS84).}

\item{lng}{Numeric. Longitude of the delineation pour point (in WGS84).}

\item{rcode}{Character. Two-letter state or region code (e.g., \code{"WI"}, \code{"IL"}, \code{"NY"}).}

\item{basin_group}{Character. Group name for the watershed boundary. Defaults to \code{"StreamStats Basin"}.}

\item{stream_group}{Character. Group name for stream flowlines. Defaults to \code{"Stream Flowlines"}.}

\item{layer_group}{Character. Fallback group name for backward compatibility.}

\item{stream_color}{Character. Color for the stream flowlines. Defaults to \code{"#0055ff"}.}

\item{basin_color}{Character. Fill and stroke color for the watershed boundary polygon. Defaults to \code{"#3388ff"}.}

\item{color}{Optional character fallback color for backward compatibility.}

\item{weight}{Numeric. Base stroke weight (line thickness) for streams in pixels. Defaults to \code{3}.}

\item{opacity}{Numeric. Opacity of stream flowlines (0 to 1). Defaults to \code{0.9}.}

\item{fillOpacity}{Numeric. Opacity of watershed polygon fill (0 to 1). Defaults to \code{0.15}.}

\item{include_flowlines}{Logical. Whether to fetch and render NHD stream flowlines. Defaults to \code{TRUE}.}

\item{buffer_km}{Numeric. Search buffer radius in km around pour point to capture intersecting stream flowlines. Defaults to \code{3}.}

\item{...}{Additional arguments passed to \code{\link[leaflet]{addPolygons}} or \code{\link[leaflet]{addPolylines}}.}
}
\value{
A modified \code{leaflet} or \code{leafletProxy} map object with the StreamStats basin and flowlines added.
}
\description{
Queries the USGS StreamStats SS-Delineate API for a specified location and state/region code
to delineate the watershed drainage basin, extracts intersecting vector stream flowlines via
\code{nhdplusTools}, and adds them to a Leaflet map object.
}
\examples{
\dontrun{
library(leaflet)
leaflet() |>
    addTiles() |>
    add_streamstats_layer(lat = 43.0731, lng = -89.4012, rcode = "WI")
}
}
