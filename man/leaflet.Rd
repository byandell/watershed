% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaflet.R
\name{leaflet}
\alias{leaflet}
\alias{build_base_map}
\alias{add_draw_toolbar}
\alias{get_huc_from_point}
\alias{get_huc_col}
\alias{aggregate_hucs}
\alias{get_hucs_from_polygon}
\alias{add_leaflet_hex_overlay}
\alias{add_leaflet_flowlines}
\alias{add_watershed_legend}
\title{Interactive Leaflet Geographic Utilities}
\usage{
build_base_map()

add_draw_toolbar(map)

get_huc_from_point(lng, lat)

get_huc_col(df)

aggregate_hucs(hucs_sf, target_level = 8)

get_hucs_from_polygon(polygon_sf, huc_level = 8, max_hucs = 10)

add_leaflet_hex_overlay(
  map,
  hex_obj,
  hex_color = "#7F8C8D",
  bound_color = "#8E44AD"
)

add_leaflet_flowlines(
  map,
  flowlines,
  stream_color = "#0055ff",
  group = "Stream Flowlines"
)

add_watershed_legend(
  map,
  show_hex = TRUE,
  show_streams = TRUE,
  show_habitat = TRUE,
  show_legend = TRUE,
  position = "bottomright"
)
}
\arguments{
\item{map}{A `leaflet` map object or `leafletProxy` handle.}

\item{lng}{Numeric longitude coordinate}

\item{lat}{Numeric latitude coordinate}

\item{df}{A data frame or `sf` object containing HUC columns.}

\item{hucs_sf}{An `sf` data frame of fine HUC polygon geometries.}

\item{target_level}{Target HUC digit level (2-12, numeric or character, default: 8).

Helper to resolve HUC ID column name across all HUC levels}

\item{polygon_sf}{An `sf` or `sfc` polygon or point object representing a drawn rubberband region or marker.}

\item{huc_level}{Target USGS HUC digit level (2, 4, 6, 8, 10, 12, or character e.g. "huc08"). Single-digit numbers are padded ("huc02", "huc04", "huc06", "huc08").}

\item{max_hucs}{Maximum number of subwatersheds before scaling up to broader HUC levels (default: 10, targeting 5-10 regions).}

\item{hex_obj}{A `watershed_hex_overlay` S3 object (or a list containing `layer` and `hex_overlay` sf objects).}

\item{hex_color}{Stroke color for hexagonal grid cells (default: "#C0392B").}

\item{bound_color}{Stroke color for watershed boundary (default: "#2980B9").}

\item{flowlines}{An `sf` data frame containing LINESTRING geometries of stream flowlines.}

\item{stream_color}{Stroke color for flowlines (default: "#0055ff").}

\item{group}{Group name for layer controls (default: "Stream Flowlines").}

\item{show_hex}{Logical. Include hex grid in legend.}

\item{show_streams}{Logical. Include stream flowlines in legend.}

\item{show_habitat}{Logical. Include habitat/landmarks in legend.}

\item{show_legend}{Logical. Whether to show or remove legend.}

\item{position}{Position on map (default: "bottomright").}
}
\value{
`build_base_map`: A `leaflet` HTML widget object.

A Leaflet map object with draw toolbar attached.

`get_huc_from_point`: An `sf` polygon representation of the covering HUC12.

Character string of the identified HUC column name.

`aggregate_hucs`: An `sf` data frame of aggregated parent HUC geometries.

`get_hucs_from_polygon`: An `sf` data frame of overlapping HUC subwatersheds (auto-scaled via area & code-annealed prefix matching).

`add_leaflet_hex_overlay`: Updated `leaflet` map object.

Updated `leaflet` map object.

Updated `leaflet` map object.
}
\description{
Helper utilities for building interactive Leaflet base maps with search capabilities,
reverse-geocoding points to USGS HUC12 subwatershed boundaries, and rendering spatial
hexagonal grid overlays.

Combines fine HUC polygons (e.g. HUC12s) into broader HUC levels (HUC10, HUC08, HUC06, HUC04, HUC02)
by prefix matching and spatial union without requiring additional spatial network requests.
}
